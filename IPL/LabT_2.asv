close all;
clear all;
clc;

img=imread('capture.jpg');
subplot(2,2,1);
imshow(img);
title ('RGB image');
hold on;

imgR=img(:,:,1);
subplot(2,2,2), imshow(imgR);
imgG=img(:,:,2),imshow(imgR);
imgGra